/* Global types */

export type Person = {
  district: string;
  link: string;
  name: string;
  office: string;
  party: string;
  phone: string;
  state: string;
};

export type Option = {
  label: string;
  value: string;
};
